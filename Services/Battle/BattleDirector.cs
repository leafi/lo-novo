using System;

namespace lo_novo
{
    // Directs battles for/over multiple Rooms. Keeps state and figures out when to inject new NPCs.
    // Requires much co-operation from & should be driven by the Room.
    // Provides battle services to the Room script.
    // It may or may not need to be aware that multiple Rooms can drive it.
    public class BattleDirector
    {
        public BattleDirector()
        {
        }
    }
}

